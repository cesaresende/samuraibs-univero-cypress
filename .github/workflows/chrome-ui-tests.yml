name: Chrome UI Testes

on:
  workflow_dispatch:
  
jobs:

  chrome-ui-tests:
  runs-on: ubuntu-latest
  
  steps:
  
    - name: Checkout
      uses: actions/checkout@v3
      
    - name: List files project 
      run: ls -l 
